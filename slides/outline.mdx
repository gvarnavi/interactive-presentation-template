---
id: outline
sidebar_position: 1
title: Introduction
hide_table_of_contents: true
hide_title: true
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

import TwoNonconvexSetsComponent from '@site/src/components/TwoNonconvexSetsComponent.js';
    
export const packages = {
        official: ['matplotlib'],
	micropip: ['python-cowsay'],
}

# Runnable Code (Blocks)

<Tabs Lazy>

  <TabItem value="python" label="Python code" default >

	Markdown preformatted code blocks with the `python` language identifier are
	automatically converted to runnable and editable code blocks thanks
	to [react-py](https://github.com/elilambnz/react-py).
	See the react-py repo and documentation for more info.

	For example, in a Markdown docs file:

	````
	```python
	import numpy as np
	
	print("Hello, World!")
	print(np.arange(10))
	```
	````

	Becomes:

	```python
	import numpy as np
	
	print("Hello, World!")
	print(np.arange(10))
	```

	We have included numpy in our default install configuration, but if you want any additional 
	packages you can specify them using a comma-separated list, per code-block:

	````
	```python packages="scipy python-cowsay"
	from scipy import special
	from cowsay import cowsay

	num = 27
	res = special.cbrt(num)
	message = f"The cube root of {num} is {res}"
	print(cowsay(message))
	```
	````

	```python packages="scipy python-cowsay"
	from scipy import special
	from cowsay import cowsay

	num = 27
	res = special.cbrt(num)
	message = f"The cube root of {num} is {res}"
	print(cowsay(message))
	```
  </TabItem>

  <TabItem value="jsx" label="Javascript markup">
	
	Markdown preformatted code blocks with the `jsx live` language identifier are
	automatically converted to runnable and editable code blocks thanks
	to [@docusaurus/theme-live-codeblock](https://docusaurus.io/docs/markdown-features/code-blocks#interactive-code-editor).

	````
	```jsx live
	function ReptileListItems() {
	  const reptiles = ["alligator", "snake", "lizard"];
	  return reptiles.map((reptile) => <li key={reptile}>{reptile}</li>);
	}
	```
	````

	```jsx live
	function ReptileListItems() {
	  const reptiles = ["alligator", "snake", "lizard"];
	  return reptiles.map((reptile) => <li key={reptile}>{reptile}</li>);
	}
	```

  </TabItem>
	

  <TabItem value="observable" label="Observable notebooks">
	
	Finally, you can embed Observable notebooks as React components directly, as described [here](https://observablehq.com/documentation/embeds/how-to-embed-a-notebook-in-a-react-app).

    	<TwoNonconvexSetsComponent />

  </TabItem>

</Tabs>

